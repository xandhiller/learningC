#include <stdio.h>

int main (void)
{
    int x, y, array[10];
    int x = 0;
    int y = NULL;

        
    if( x == y )
    {
        printf("x and y are equal\n");
    }
    else(printf("0 and NULL are not equal\n");

    if(array[x] == array[y])
    {
        printf("array[0] is the same as array[NULL]");
    }
    else
    printf("array[0] is NOT the same as array[NULL]");

    return 0;
}
